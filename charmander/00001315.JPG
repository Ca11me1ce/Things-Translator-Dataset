
import cv2
from glob2 import glob
n=0
for fn in glob('*.jpg'): #确认文件格式
    img=cv2.imread(fn)
    horizontal_img=cv2.flip(img,0)#选择旋转类型
    splitName=fn.split(".")
    newName=splitName[0]
    cv2.imwrite(newName+'_flip0.jpg',horizontal_img)
    print("Finished, ", n)
    n+=1